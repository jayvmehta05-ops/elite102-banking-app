# config.py
# Put your MySQL login info here.
# Every other file imports from this so you only have to change it once.
# Never upload this file to GitHub because it has your password in it.

DB_NAME = "banking_system"
 
DB_CONFIG = {
    "host":     "localhost",   # or "127.0.0.1"
    "user":     "root",        # your MySQL username
    "password": "gcevCEAH5QxN?sbK",  # ← replace with your actual password
    "database": DB_NAME,
}